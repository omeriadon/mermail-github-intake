# Demo

The final bounty recording plan is maintained in [`VIDEO.md`](VIDEO.md).

Use that file as the single source of truth for:

- the exact live Codex prompt;
- the real Mermail → GitHub workflow to record;
- the explicit approval step;
- independent GitHub verification;
- the unsent Mermail acknowledgement step;
- editing targets and the final single X post.

The credential-free pre-write smoke test is recorded in [`LIVE_TEST.md`](LIVE_TEST.md).

For deterministic, non-network proof:

```bash
npm test
npm run demo
```

The current proof suite covers adversarial intake, semantic and exact-source duplicates, unsafe scans, materially partial evidence, changed-effect re-preview, ambiguous-write reconciliation with zero automatic retries, and a feature request that remains actionable without invented environment/reproduction boilerplate.
